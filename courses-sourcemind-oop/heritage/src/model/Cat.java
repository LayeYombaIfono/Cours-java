/*
    HERITAGE DE CLASSE
    ● Une classe peut étendre une autre classe
    ● Une sous-classe hérite des membres de
    la superclasse
    ● Une sous-classe peut remplacer les méthodes de
        sa superclasse
    ● Animal = superclasse
    ● Chat = sous-classe
    ● Une classe peut également implémenter une
    interface
    ● Une interface est comme une classe mais
    sans implémentation
    ● Une classe doit remplacer les méthodes de
    l'interface qu'elle implémente
 */

package model;

public class Cat extends Animal implements Walkable{

}
